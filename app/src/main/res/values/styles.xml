<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="AppTheme.Base"/>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeCloseDrawable">@drawable/ic_action_check</item>
    </style>

    <style name="AppTheme.NoActionBar.Compat.Translucent" parent="AppTheme.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyCardViewStyle" parent="CardView">
        <item name="cardCornerRadius">@dimen/card_view_corner_radius</item>
        <item name="android:layout_marginTop">@dimen/card_view_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/card_view_vertical_margin</item>
        <item name="android:layout_marginRight">@dimen/card_view_horizontal_margin</item>
        <item name="android:layout_marginLeft">@dimen/card_view_horizontal_margin</item>

        <!-- android:clickable has to be true or onClick-method must be implemented -->
        <item name="android:foreground">?attr/selectableItemBackground</item>
    </style>

</resources>